# Alina Bot — Python dependencies
# Pin minor versions; patch versions updated via Dependabot / manual review.

aiogram==3.7.0          # Telegram Bot API framework
aiohttp==3.10.5         # Async HTTP client (shared session across providers)
sqlalchemy==2.0.35      # ORM + async engine
asyncpg==0.29.0         # PostgreSQL async driver
python-dotenv==1.0.1    # .env file loading
apscheduler==3.10.4     # Periodic job scheduling
